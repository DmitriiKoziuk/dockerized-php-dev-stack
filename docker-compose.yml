version: '2'

services:
    nginx:
        image: nginx:latest
        ports:
            - "80:80"
            - "443:443"
        volumes:
            - ./hosts:/etc/nginx/conf.d
            - ./www:/var/www
            - ./logs:/var/log/nginx
        links:
            - php73dev
            - php73prod
    php73dev:
        build: ./images/php73dev
        links:
            - mysql8
        volumes:
            - ./www:/var/www
            - /usr/local/etc/php/conf.d
            - ./php/php73dev/conf.d/custom.ini:/usr/local/etc/php/conf.d/custom.ini
    php73prod:
        build: ./images/php73prod
        links:
            - mysql8
        volumes:
            - ./www:/var/www
            - /usr/local/etc/php/conf.d
            - ./php/php73prod/conf.d/custom.ini:/usr/local/etc/php/conf.d/custom.ini
    mysql8:
        image: mysql:8
        ports:
            - "3306:3306"
        volumes:
            - ./mysql/8/db:/var/lib/mysql
            - /usr/local/bin
        environment:
            MYSQL_ROOT_PASSWORD: secret
